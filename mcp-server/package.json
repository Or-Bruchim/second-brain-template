{
  "name": "brain-mcp",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "MCP server exposing Second Brain wiki (Quartz) to Claude Code — local file search by default, remote /api/search + /api/chat as optional fallback.",
  "main": "index.mjs",
  "bin": {
    "brain-mcp": "./index.mjs"
  },
  "scripts": {
    "start": "node index.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "gray-matter": "^4.0.3",
    "zod": "^3.24.0"
  }
}
